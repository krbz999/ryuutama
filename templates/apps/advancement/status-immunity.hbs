<form>
  <fieldset>
    <legend>{{title}}</legend>
    {{formGroup fields.choice.fields.chosen value=advancement.choice.chosen labelAttr="name" classes="slim" rootId=rootId}}
    <p class="hint">{{localize "RYUUTAMA.PSEUDO.ADVANCEMENT.STATUS.hint"}}</p>
  </fieldset>
</form>
