<form>
  <fieldset>
    <legend>{{title}}</legend>
    {{formGroup fields.choice.fields.chosen.fields.stamina value=advancement.choice.chosen.stamina rootId=rootId}}
    {{formGroup fields.choice.fields.chosen.fields.mental value=advancement.choice.chosen.mental rootId=rootId}}
    <p class="hint">{{localize "RYUUTAMA.PSEUDO.ADVANCEMENT.RESOURCE.hint"}}</p>
  </fieldset>
</form>
