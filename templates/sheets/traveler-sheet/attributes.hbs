<section data-tab="{{tabs.attributes.id}}" class="{{tabs.attributes.cssClass}}" data-group="{{tabs.attributes.group}}">
  {{!-- ABILITIES --}}
  <div class="icon-fields">
    {{#each abilities}}
    <div class="icon-field">
      {{#if @root.isEditable}}
      <button type="button" data-action="configure" data-config="ability" data-ability="{{ability}}" class="icon fa-solid fa-cog"></button>
      {{else}}
      <ryuutama-icon src="{{icon}}"></ryuutama-icon>
      {{/if}}
      <div class="form-group stacked">
        <label>{{label}}</label>
        <div class="form-fields">
          <span class="value">{{value.die}}</span>
        </div>
      </div>
    </div>
    {{/each}}
  </div>

  {{!-- HP & MP --}}
  <div class="form-group wide">
    <label>{{localize "RYUUTAMA.ACTOR.HP"}}</label>
    <div class="form-fields">
      <resource-bar name="system.resources.stamina.value" class="stamina"></resource-bar>
      {{#unless disabled}}
      <button type="button" data-action="configure" data-config="resource" data-resource="stamina" class="icon fa-solid fa-cog"></button>
      {{/unless}}
    </div>
  </div>
  <div class="form-group wide">
    <label>{{localize "RYUUTAMA.ACTOR.MP"}}</label>
    <div class="form-fields">
      <resource-bar name="system.resources.mental.value" class="mental"></resource-bar>
      {{#unless disabled}}
      <button type="button" data-action="configure" data-config="resource" data-resource="mental" class="icon fa-solid fa-cog"></button>
      {{/unless}}
    </div>
  </div>

  {{!-- FUMBLES & EXP --}}
  <div class="icon-fields">
    <div class="icon-field">
      <ryuutama-icon src="systems/ryuutama/assets/official/icons/check/fumble.svg"></ryuutama-icon>
      <div class="form-group stacked">
        <label for="{{rootId}}-system.fumbles.value">{{systemFields.fumbles.fields.value.label}}</label>
        <div class="form-fields">
          <input type="text" name="system.fumbles.value" class="delta" value="{{ifThen disabled document.system.fumbles.value source.system.fumbles.value}}" {{disabled (not editable)}} placeholder="0" id="{{rootId}}-system.fumbles.value">
        </div>
      </div>
    </div>

    <div class="icon-field">
      <ryuutama-icon src="icons/svg/coins.svg"></ryuutama-icon>
      <div class="form-group stacked">
        <label for="{{rootId}}-system.exp.value">{{systemFields.exp.fields.value.label}}</label>
        <div class="form-fields">
          <input type="text" name="system.exp.value" id="{{rootId}}-system.exp.value" class="delta" value="{{ifThen disabled document.system.exp.value source.system.exp.value}}">
        </div>
      </div>
    </div>
  </div>

  {{!-- ATTACKS & CHECKS --}}
  <div class="icon-fields">
    <div class="icon-field">
      <img src="{{weaponImage}}" alt="">
      <div class="attack">
        <button type="button" data-action="rollCheck" data-check="accuracy" {{disabled (not editable)}}>{{localize "RYUUTAMA.ACTOR.rollAccuracy"}}</button>
        <button type="button" data-action="rollCheck" data-check="damage" {{disabled (not editable)}}>{{localize "RYUUTAMA.ACTOR.rollDamage"}}</button>
      </div>
    </div>

    <button type="button" class="heightened" data-action="rollCheck" data-check="check" {{disabled (not editable)}}>
      <i class="fa-solid fa-burst"></i>
      <span>{{localize "RYUUTAMA.ACTOR.rollCheck"}}</span>
    </button>
  </div>

  {{!-- STATUSES --}}
  <div class="icon-fields column-3">
    {{#each statuses}}
    <div class="icon-field">
      <a data-action="toggleStatus" data-status="{{status}}">
        <ryuutama-icon src="{{img}}" {{#if active}} class="active" {{/if}}></ryuutama-icon>
      </a>
      <div class="form-group stacked">
        <label>{{name}}</label>
        <div class="form-fields">
          <span class="value italics">{{label}}</span>
        </div>
      </div>
    </div>
    {{/each}}
  </div>

  {{!-- CONDITION & ARMOR --}}
  <div class="icon-fields">
    {{!-- CONDITION --}}
    <div class="icon-field">
      {{#if isEditable}}
      <button type="button" data-action="configure" data-config="condition" class="icon fa-solid fa-cog"></button>
      {{else}}
      <a data-action="rollCheck" data-check="condition">
        <ryuutama-icon src="systems/ryuutama/assets/official/icons/check/condition.svg"></ryuutama-icon>
      </a>
      {{/if}}
      <div class="form-group stacked">
        <label>{{localize "RYUUTAMA.ACTOR.condition"}}</label>
        <div class="form-fields">
          <span class="value italics">{{ifThen document.system.condition.value document.system.condition.value 0}}</span>
          {{#if conditionShape.active}}
          <div class="tags">
            {{#if conditionShape.high}}
            {{#if disabled}}
            <span class="tag">{{systemFields.condition.fields.shape.fields.high.label}}: {{conditionShape.ability}}</span>
            {{else}}
            {{formGroup systemFields.condition.fields.shape.fields.high value=source.system.condition.shape.high blank=(localize "RYUUTAMA.TRAVELER.conditionShapeNoneSelected") hint=false}}
            {{/if}}
            {{else if conditionShape.low}}
            <span class="tag">{{localize "RYUUTAMA.TRAVELER.conditionShapeOutOfShape"}}</span>
            {{/if}}
          </div>
          {{/if}}
        </div>
      </div>
    </div>

    {{!-- ARMOR --}}
    <div class="icon-field">
      {{#if isEditable}}
      <button type="button" data-action="configure" data-config="defense" class="icon fa-solid fa-cog"></button>
      {{else}}
      <ryuutama src=""></ryuutama>
      {{/if}}
      <div class="form-group stacked">
        <label>{{localize "RYUUTAMA.ACTOR.armor"}}</label>
        <div class="form-fields">
          {{#if disabled}}
          <span class="value italics">{{document.system.defense.total}}</span>
          {{else}}
          {{formInput systemFields.defense.fields.armor value=source.system.defense.armor placeholder="0"}}
          {{/if}}
          {{#if armor.hasTags}}
          <div class="tags">
            {{#if armor.tags.physical}}
            <span class="tag">{{localize "RYUUTAMA.ACTOR.physical"}}: {{armor.tags.physical}}</span>
            {{/if}}
            {{#if armor.tags.magical}}
            <span class="tag">{{localize "RYUUTAMA.ACTOR.magical"}}: {{armor.tags.magical}}</span>
            {{/if}}
          </div>
          {{/if}}
        </div>
      </div>
    </div>
  </div>

</section>
